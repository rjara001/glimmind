{
  "name": "glimmind---word-association-master",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "emulators": "firebase emulators:start --only auth,firestore",
    "dev:emu": "concurrently \"npm run emulators\" \"npm run dev\"",
    "setup:emu": "firebase emulators:start --only auth,firestore --import=./emulator-data && firebase emulators:start --only auth,firestore",
    "seed:emu": "node scripts/setup-emulator-data.js"
  },
  "dependencies": {
    "firebase": "^12.8.0",
    "firebase-admin": "^13.6.0",
    "next": "^16.1.3",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^9.2.1",
    "typescript": "~5.8.2",
    "vite": "^6.4.1"
  }
}
